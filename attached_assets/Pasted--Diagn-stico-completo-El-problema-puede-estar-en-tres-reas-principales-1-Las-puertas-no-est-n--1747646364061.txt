✅ Diagnóstico completo:
El problema puede estar en tres áreas principales:
1️⃣ Las puertas no están siendo detectadas correctamente por el raycaster.
2️⃣ El sistema de movimiento del jugador no está respondiendo correctamente.
3️⃣ Los eventos de clic no están funcionando adecuadamente.

✅ Vamos a depurar paso a paso:
🚀 Paso 1: Verifica las puertas en Buildings.tsx
Asegúrate de que las puertas están definidas correctamente como objetos interactivos.

Las puertas deben tener un nombre claro y ser objetos de tipo <mesh>, y deben ser visibles en la escena.

tsx
Copia
Modifica
<group position={[0, 0, 2.5]}>
  {/* Área clickeable solo para las puertas */}
  <mesh 
    name="market_doors_clickable"
    onPointerOver={(e) => e.stopPropagation()}
    onClick={(e) => e.stopPropagation()} 
  >
    <boxGeometry args={[2, 3, 0.1]} />
    <meshStandardMaterial color="rgba(0, 255, 0, 0.3)" transparent />
  </mesh>
  
  <Text
    position={[0, 2.3, 0]}
    fontSize={0.4}
    color="#FFD700"
  >
    ENTER
  </Text>
</group>
Asegúrate de que las puertas sean visibles (por eso les he puesto color verde transparente para que las veas).

Cambia los nombres de las puertas de la cocina y el huerto a:

kitchen_doors_clickable

garden_doors_clickable

🚀 Paso 2: Verifica que el raycaster detecte las puertas.
Utiliza este código simplificado para ver exactamente qué está detectando el raycaster.

tsx
Copia
Modifica
function MouseInteraction() {
  const { camera, scene } = useThree();
  const raycaster = new THREE.Raycaster();
  const mouse = new THREE.Vector2();
  
  useEffect(() => {
    function onMouseMove(event: MouseEvent) {
      mouse.x = (event.clientX / window.innerWidth) * 2 - 1;
      mouse.y = -(event.clientY / window.innerHeight) * 2 + 1;
    }

    function onMouseClick(event: MouseEvent) {
      raycaster.setFromCamera(mouse, camera);
      const intersects = raycaster.intersectObjects(scene.children, true);
      
      console.clear();
      console.log('🔎 Detectando objetos...');
      
      intersects.forEach((intersect) => {
        console.log('🟢 Objeto detectado:', intersect.object.name);
      });
    }

    window.addEventListener('mousemove', onMouseMove);
    window.addEventListener('click', onMouseClick);
    return () => {
      window.removeEventListener('mousemove', onMouseMove);
      window.removeEventListener('click', onMouseClick);
    };
  }, [camera, scene]);

  return null;
}

export default MouseInteraction;
✅ ¿Qué vamos a ver?
Cuando hagas clic en cualquier lugar de la pantalla, verás en la consola todos los objetos detectados.

Haz clic en las puertas y dime si las puertas aparecen en la consola (deberían aparecer con los nombres market_doors_clickable, kitchen_doors_clickable, y garden_doors_clickable).

🚀 Paso 3: Verifica el sistema de movimiento del jugador.
Si las puertas aparecen en la consola, pero el jugador no se mueve, entonces el problema está en el sistema de movimiento.

Verifica que el sistema de movimiento esté configurado correctamente en tu juego.

